# Premium Mobile Bottom Menu

Replace only:
`app/globals.css`

No SQL and no other file changes.

The bottom menu is now:
- centered correctly on the screen;
- one floating rounded container;
- equal-width navigation buttons;
- safe-area aware on iPhone;
- subtle shadow and glass effect;
- active page gets its own inset card and small indicator;
- responsive down to narrow phones.

Commit → Push origin → Vercel Ready → refresh the phone page.
